# PRD — Personal Local Meeting/Interview Assist Tool ("Parakeet Clone")

**Owner:** Jon
**Status:** v1.6 — Phase 1 (MVP) complete as of Day 11 (2026-09-03), success criteria in §11 met. Phase 2 (overlay) fully done and physically confirmed as of Day 18 (2026-09-06). Phase 2.5's real-time streaming transcript (base.en) was built, then fully reverted back to segment-based small.en after real accuracy bugs — see §8 Phase 2.5. STT model-size re-evaluation (Day 18.5) concluded: keep small.en. Live-agent-listening (Day 18.8-18.10) confirmed viable unattended over a real ~50-minute run (2026-09-07) — a real second-mode candidate, product decision on shipping it still pending. Mic capture (Phase 3, pulled forward, Day 19) implemented and real-tested 2026-09-07 — found and fixed 3 real bugs (mic-noise hallucination, transcribe concurrency contention, and a suggestion-quality regression traced to the claude CLI subprocess auto-loading this project's own CLAUDE.md); the suggestion fix is live-verified, the other two await a real mic re-test.
**Scope:** Strictly personal use. Not for distribution, sale, or third-party use.

---

## 1. Overview

A Windows desktop tool that listens to system audio during a live call (work meeting, mock interview, or real interview), transcribes it locally in real time, and — on a trigger — asks a locally-invoked Claude (via the Claude Code CLI, using Jon's existing Claude.ai subscription, not a paid API key) to generate a suggested response. The suggestion appears in an app window Jon can glance at during the call.

A post-call summary feature is explicitly a later phase, not part of the MVP.

## 2. Goals

- Real-time-enough suggested responses during live work meetings and interviews (mock, and eventually real).
- Run entirely locally on Windows: local speech-to-text, local audio capture, Claude invoked through the CLI using an existing subscription — no per-token API billing.
- Low-friction display: a normal window to start, a toggleable always-on-top overlay later.
- Nothing about this needs to look or behave like a shippable product — it needs to work reliably for one user, on one machine.

## 3. Non-Goals

- Not distributed, packaged as an installer, or sold to anyone else.
- Not integrating with specific meeting platforms (Zoom/Teams/Meet APIs) — audio capture is platform-agnostic via system loopback.
- Not building disclosure/consent-management features — that's a decision Jon makes per-use, not something the app automates or enforces.
- Not multi-user, not cloud-synced, no accounts.
- Speaker diarization, meeting-bot integrations, and a polished installable app are out of scope unless a later phase explicitly adds them.

## 4. Users & Use Cases

Single user (Jon). Three use modes, same underlying pipeline:

1. **Work meetings** (primary) — live assist during calls, eventually with a post-meeting summary of what was discussed.
2. **Mock interviews** — practice runs, live or reviewed after.
3. **Real job interviews** — used live, as interview practice.

The suggestion prompt should differ by mode (a work-meeting suggestion looks different from an interview-answer suggestion), so "mode" is a first-class setting, not an afterthought.

## 5. Legal & Ethical Considerations (acknowledged, not re-litigated)

Flagged once during planning, carried into the design rather than ignored:


- The Philippines' Anti-Wiretapping Act (RA 4200) generally requires consent to record private communications. This shapes one concrete design default: **the MVP does not persist raw audio or transcripts to disk.** Everything lives in memory for the duration of a session and is discarded on close unless a later phase adds an explicit, opt-in "save this session" action.

This isn't legal advice, just a design constraint carried from the earlier discussion.

## 6. System Architecture

**Revised after Day 0 feasibility testing** (see §12 for the log). WSL cannot capture Windows system audio loopback — WSLg's PulseAudio bridge only carries WSL-internal GUI-app audio, never native Windows app output. This forces a two-process, two-OS-environment architecture:

```
Windows-native process                          WSL process
------------------------                        -----------
WASAPI loopback capture      --PCM frames-->     VAD-segmented local STT (faster-whisper)
Global hotkey listener       --trigger evt-->    Pause/silence trigger detection
PySide6 window                                   Long-lived `claude` CLI subprocess
 (transcript + suggestions)  <--text-------      (stream-json mode)
                              local TCP socket,
                              WSL-side listens,
                              Windows-side connects
                              (WSL2 localhost forwarding)
```

Key components:

- **Audio capture (Windows-native):** WASAPI loopback via `pyaudiowpatch` or `soundcard`, run as a native Windows Python process — not inside WSL. Captures what's playing out of speakers/headphones (the other participant's side). Own-mic capture is deferred to the summary phase (§8, Phase 3).
- **GUI (Windows-native):** PySide6 window (transcript pane + suggestions pane) runs in the *same* native Windows process as audio capture, not via WSLg. Decided this way specifically so Phase 2's always-on-top/click-through overlay doesn't require relocating the GUI later — it's already native.
- **IPC:** a local TCP socket between the two processes. WSL-side listens on `127.0.0.1:<port>`; the Windows-side process connects to it as a client, relying on WSL2's default localhost-forwarding (a port opened inside WSL is reachable from Windows via `localhost`) — the well-supported direction, versus WSL initiating a connection out to a Windows-hosted port. Two lightweight message types: raw PCM audio frames (Windows → WSL) and JSON control/text messages (both directions: transcript segments and suggestions WSL → Windows; hotkey trigger events and mode/settings changes Windows → WSL).
- **Local STT (WSL):** `faster-whisper` (`small.en`, CPU, int8), chunked by voice-activity detection rather than fixed time windows.
- **Claude invocation (WSL):** one long-lived `claude` CLI process per session in headless streaming mode (`--input-format stream-json --output-format stream-json`, confirmed viable in Day 0) rather than spawning a fresh process per suggestion — avoids per-call CLI cold-start overhead. Context (mode, rolling transcript window) is managed by our app and streamed in, not the CLI's own session/`--continue` behavior, so meetings never cross-contaminate.
- **Trigger logic:** pause detection runs on the WSL side (it already sees the audio stream for STT); the manual hotkey is captured natively on Windows and forwarded to WSL over the control socket.
- **One repo, one dev workflow:** both sides live in the same git repo (`windows_app/`, `wsl_app/`), authored from a single `claude` CLI session run in WSL. The Windows-native component doesn't need a second Claude Code setup — it's just Python code that gets *executed* by a native Windows Python install. WSL can invoke Windows binaries directly (`python.exe`) via its built-in interop, so the Windows-side process can typically be launched right from the WSL shell without opening a separate terminal.

## 7. Open Decisions Needing Your Sign-off

I'm proposing defaults below so we're not stalled on every micro-choice — flag any you want changed before I write the dev plan:

| # | Decision | Proposed default | Why |
|---|---|---|---|
| 1 | STT engine/model | `faster-whisper`, `small.en`, CPU-only (int8 quantized for speed) | Confirmed: AMD GPU, no CUDA — no local GPU acceleration path for faster-whisper, so we tune for CPU speed instead |
| 2 | GUI framework | Python + PySide6 (Qt) | Native Windows always-on-top/click-through overlay support later, same language as the audio/STT stack, no second runtime |
| 3 | Trigger mechanism | Auto-pause-detection + manual hotkey, both enabled | Maximizes responsiveness without losing manual control |
| 4 | Claude session model | One long-lived streaming CLI process per meeting session, app-managed rolling context | Avoids per-call cold-start latency; avoids cross-session context bleed |
| 5 | Persistence | Off by default in MVP; nothing written to disk unless explicitly enabled later | Matches the legal/privacy consideration in §5 |
| 6 | Windows-only for now | Confirmed | You confirmed this already — listed here for completeness |
| 7 | Build workflow | Single `claude` CLI session, run in WSL, authors both `windows_app/` and `wsl_app/` in one repo. Windows-side code is executed via WSL→Windows interop (`python.exe`) or a separate Windows terminal | Confirmed Day 0: `claude` works in WSL with subscription auth. No need for a second Claude Code setup just because part of the runtime is Windows-native |
| 8 | Repo | Local git repo + private GitHub remote | Version history and off-machine backup, kept private. Created in Day 0: `github.com/jjaguilar08/adarna-ai`, nothing committed yet |
| 9 | Python version | 3.10.12 (WSL's existing version) — no upgrade to 3.11 | Nothing in the stack (faster-whisper, PySide6, sockets/asyncio) actually requires 3.11+; that was an arbitrary default on my part, not a real requirement |
| 10 | Audio capture location | Native Windows process (confirmed necessary, not optional) | Day 0 proved WSLg's PulseAudio bridge can't see native Windows app audio — `RDPSink.monitor` only carries WSL-internal GUI-app sound |
| 11 | GUI location | Native Windows process, same one as audio capture | Since a native Windows component is now required anyway, building the GUI there too avoids relocating it for Phase 2's overlay later |
| 12 | IPC mechanism | Local TCP socket, WSL-side listens, Windows-side connects (WSL2 localhost forwarding) | The better-supported connection direction for WSL2; avoids relying on the WSL virtual-switch gateway IP from the Windows side |

## 8. Functional Requirements by Phase

### Phase 1 — Live Assist Core (MVP)
- Start/stop a session from the app window.
- Live system-audio capture via WASAPI loopback, with a device picker and a "test capture" button.
- Local streaming STT producing a visible rolling transcript.
- Mode toggle: "Work Meeting" vs "Interview" (changes the suggestion system prompt/tone).
- Auto-trigger on pause + manual hotkey trigger, both feeding the long-lived Claude CLI subprocess.
- Suggested responses appear in-window, appended to a running list for that session (so you can scroll back within a live call). **Superseded Day 13 — see Phase 1.5b below.**
- One-click copy-to-clipboard for any suggestion.
- Settings: audio device, Whisper model size, mode, silence threshold.
- No disk persistence of audio or transcript by default.

### Phase 1.5 — Suggestion & Context Refinements
Added post-MVP (2026-09-03), based on real usage of the Phase 1 build. Touches core suggestion logic only — no UI relocation — done before Phase 2 so the overlay is built against the improved behavior rather than needing rework afterward.

- **Pre-session context notes:** a multi-line text box in the settings panel to paste free-form notes before starting a session — a CV/job description for interviews, a PRD/agenda for meetings. Sent once as part of `settings_changed` (same pattern as mode/model/pause settings), included in the system prompt/context for that session only, not hot-swapped mid-session.
- **Output style change (both modes):** suggestions switch from a ready-to-read spoken script to terms/concepts/key points the user builds their own answer from — e.g. asked about OOP principles, the output is terms like "Encapsulation, Inheritance, Polymorphism, Abstraction" rather than a full sentence to say aloud. Applies to both Work Meeting and Interview modes. This is a system-prompt change, not a new message type. **Reverted Day 13 — see Phase 1.5b below.**
- **Auto-suggest on/off toggle, changeable mid-session:** when on, behaves as today (pause-triggered + hotkey/button both generate). When off, pause-triggered generation is suppressed entirely — only an explicit manual trigger (the existing hotkey, plus a corresponding in-window button) generates a suggestion, from the most recent transcript context. Toggling happens live during a running session (a new control in the window, and worth deciding during implementation whether the existing global hotkey also gets a toggle-specific variant or stays purely "generate now").

### Phase 1.5b — Output & Display Adjustments
Added 2026-09-03, after one real session's use of Phase 1.5's terms-style output and growing suggestion list. Small, targeted changes rather than a new phase of work.

- **Output style, reverted to script-style, both modes:** the terms/concepts style above didn't land well in practice — reverting to a ready-to-read script, but more detailed than Day 6's original single bare line: a few sentences (2-4) with real substance/reasoning behind them, not just one line, still short enough to skim and say in the moment.
- **Suggestion display auto-clears on each generation:** the suggestion pane now shows only the latest suggestion — generating a new one automatically replaces the previous one, rather than appending to a growing scrollable list. Trades away in-session scroll-back (Phase 1's original behavior) for a pane that never gets crowded.

### Phase 2 — Overlay + Polish
- Toggleable always-on-top overlay. Runs alongside the main window, not in place of it — both stay open independently, with a separate global hotkey to show/hide the overlay (distinct from the existing suggestion-trigger hotkey). **Core landed Day 14** (2026-09-03) — one item still open: a real physical keypress test of the overlay hotkey (and both hotkeys back-to-back) hasn't been confirmed by the user yet.
- **Visual redesign, Day 18 (2026-09-04):** the user shared a real ParakeetAI screenshot as a reference — the overlay is being restyled to match its look, and scope grew slightly from Day 14's "suggestion only" decision: the overlay now also shows the question/transcript excerpt that prompted the suggestion, displayed above the answer (a new `suggestion` protocol field carrying that context). Explicitly kept out of scope: a session timer, a manual "Clear" button, and the screenshot/chat toolbar buttons — those last two are genuinely new capabilities (screen capture + analysis, free-form chat) treated as separate future features, not part of the overlay polish work. **First implementation didn't meet the bar** — live testing found the overlay wasn't movable or scrollable at all despite three code-review rounds targeting exactly that. Revised visual spec from a second reference example: a black, semi-transparent box with bold white text and 💬/⭐️ section markers (Question/Answer), not just "dark and rounded" generically.
- **Drag/scroll fixed, Day 18 continued (2026-09-04):** root-caused to a real, documented Qt engine bug (QTBUG-8431) — `WA_TransparentForMouseEvents` cascaded across nested widgets (QScrollArea + viewport + content + labels) is unreliable against real OS-level clicks, even though it passes synthetic `QTest`/`childAt()` checks, which is exactly why the earlier "verified" drag/scroll testing gave false confidence. Fixed by abandoning the cascade entirely: a dedicated `_DragHandle` widget now owns dragging exclusively, and a plain unmodified `QScrollArea` handles scrolling. Visual restyle (black semi-transparent box, bold white text, 💬/⭐️ markers) also landed. **Confirmed 2026-09-06** by an actual physical click/drag/scroll test on the real Windows machine — passed. Day 18 is genuinely done, not just code-complete.
- **Interview-mode suggestion format revised twice, Day 18 continued:** first rewritten to a single narrative-lead + bullets + closing-line, formal-tone shape per a reference example. A real live test with a definitional question ("What is an SQL injection attack?") exposed that this single shape was too narrow — it forced a fabricated personal-project anecdote onto a question that didn't call for one. Rewritten again to branch by question type: a narrative-story shape for behavioral questions, a direct-lead-plus-categorized-bullets shape for technical/definitional questions. Re-verified against both the originally-failing question and a genuinely behavioral one in the same test run. Work Meeting mode's shorter, casual Day 15 style is untouched.
- Draggable/resizable overlay, adjustable opacity, optional click-through mode (toggleable — mutually exclusive with dragging while enabled). Landed Day 18 continued, confirmed by a real physical test 2026-09-06.
- Explicit, opt-in session persistence (transcript + suggestions saved locally) — off unless turned on. Not started.

### Phase 2.5 — Transcript & Suggestion Refinements
Added 2026-09-03, based on real usage after the overlay landed. Inserted ahead of the remaining Phase 2 polish/persistence work (drag/resize/opacity/click-through, persistence) since these were flagged as higher-priority.

- **Real-time streaming transcript — built, then fully reverted back to segment-based `small.en`.** The current pipeline waits for a VAD-detected pause (400ms silence) before transcribing a whole closed segment with Whisper — this originally read as slow and chunky compared to something like Google Meet's live captions, and fast speech in particular didn't transcribe accurately under the batch-per-segment approach. **Feasibility spike done Day 16, implemented Day 17, live-tested and confirmed working Day 17 (2026-09-04).** Real numbers from the spike: `faster-whisper` has no incremental/cached decoding, so true ~1s word-level captions aren't viable on this hardware; the practical ceiling was `base.en` at a ~1.5-2s partial-update cadence, with a 68-95% revision rate (most partial words get corrected at least once before settling) — a genuine, expected flicker, not a bug, since this is a batch model repeatedly re-decoding a growing window rather than a purpose-built streaming acoustic model like Google Meet's.
  **Day 18 continued (2026-09-04): reverted.** In real live use, the streaming architecture kept surfacing hallucinations and accuracy problems traced to one structural cause — decoding audio *slices*/segment boundaries rather than whole utterances, which loses context Whisper needs to get words right. A "polish hybrid" (live `base.en` streaming + a `small.en` background re-decode pass) was tried as a fix and hit three distinct real bugs, all sharing that same root cause: (1) an isolated re-decode slice lost context and produced *worse* text than the original live decode — patched with a retained rolling context window; (2) even with that fix, a session's very first segment (not enough accumulated context yet) got confidently substituted with wrong text — patched with a minimum-context floor; (3) cross-model word-timestamp misalignment between the two independently-decoded models caused duplicated words at segment boundaries — **not patched**. At that third bug, the user's own memory of the pre-streaming architecture's noticeably better accuracy was judged the more reliable signal than continuing to patch a structurally flawed approach, so the decision was made to fully revert: back to VAD-pause-then-transcribe-whole-segment, on `small.en` (not `base.en`), with `MAX_SEGMENT_SECONDS` raised from 20.0 to 60.0 after finding real interview answers commonly run 20-55s without a natural pause. Verified live and reported as materially more accurate than the streaming approach was. `wsl_app/streaming_transcriber.py` was trimmed from ~815 lines to ~164, now housing only the model-agnostic hallucination-defense functions (see below), reused by the restored segment-based flow.
  **Whisper hallucination fixes, Day 18 continued**, kept through the revert since they're independent of which architecture is active: (1) Whisper fabricating "Thank you for watching"-style text on session stop from stale/silent buffered audio — fixed with a `no_speech_prob > 0.6` segment-level filter, plus skipping the stop-time catch-up transcribe when the existing silence-timeout gate already applies; verified the underlying mechanism directly, but the user's exact end-to-end scenario couldn't be reproduced in several attempts, so this is flagged as a partial, not full, verification. (2) A second, more severe case found while verifying the first: a ~0.5s truncated mid-word audio tail produced a fully fabricated sentence with `no_speech_prob` (0.57) under the first fix's own threshold — a "not enough signal, model free-associates" failure distinct from the first "confidently silence" case. Fixed with a second, independently-calibrated filter (`avg_logprob < -2.0`, calibrated against real measured values: a pathological case at -3.32 vs. legitimate short transcriptions at -0.76 to -1.83). Re-verified live. One known, accepted residual gap: occasional small garbled artifacts (e.g. a stray "1.") on ultra-short ambiguous audio tails.
  **CPU-waste fix, Day 18 continued:** `process_tick()` was unconditionally re-transcribing unchanged audio every 1.75s for up to 6s after speech stopped, showing "still transcribing" long after the text was already correct. Fixed by skipping the transcribe call once nothing new has arrived and nothing is left tentative, with one grace-period call to let the agreement policy settle anything still tentative. Verified via direct call-count instrumentation.
- **Suggestion latency — real token-level streaming added, Day 18 continued.** Investigated the felt latency of suggestions (not cold-start, which was measured and ruled out — response length was the actual driver) and found the `claude` CLI's default `stream-json` output doesn't stream at fine granularity (a whole assistant message arrives as one event). The `--include-partial-messages` flag produces real `content_block_delta`/`text_delta` events for genuine token-by-token streaming. `ClaudeCli.ask()` gained an `on_partial_text` callback, bridged cross-thread from `MeetingSession` via `asyncio.run_coroutine_threadsafe`, so suggestions now visibly build up token-by-token in both the main window and the overlay rather than appearing all at once after the full response completes.
- **Multilingual/code-switching accuracy:** the Day 12 open item (Filipino/Taglish speech garbling under English-only models) was evaluated alongside the streaming spike. Multilingual `small` matched `small.en` exactly on clean English at no latency cost, but didn't clearly fix the Taglish test either; only `medium` showed real improvement, at ~3.5x the latency — ruled out for the live path. **Still open**: the spike's Taglish test used an English TTS voice reading Taglish text, not real Filipino-accented speech, so this isn't treated as a real answer — needs a genuine recorded sample before a final call. Day 17 stays on `base.en` for now rather than adopting an unproven multilingual switch.
- **General English accuracy — re-evaluating model size, 2026-09-05.** After the Day 18 revert, Jon flagged that plain English accuracy on `small.en` still isn't where he wants it — separate from the slice/boundary-decoding bug that's now fixed, and separate from the Taglish question above. This is worth a fresh look specifically because the architecture changed underneath the earlier decision: Day 16's "`medium` costs ~3.5x the latency" finding was measured under the *streaming* architecture, which re-decodes the whole growing buffer on every partial update — a cost that no longer applies now that decoding happens once per finished segment. A bigger local checkpoint (`medium.en`, or a distilled model like `distil-large-v3`, which claims near-`large` accuracy at closer-to-`small` speed) may be affordable under the current one-decode-per-utterance architecture even though it wasn't under streaming. **Decided 2026-09-05: stay fully local for now** (no cloud STT API — keeps PRD §2's no-per-token-billing goal intact) and benchmark a bigger local checkpoint directly with real audio before committing to a model change. A cloud STT option (e.g. OpenAI's Whisper API) is explicitly parked as a nice-to-have for later, not pursued now — see Phase 4.
  **Resolved 2026-09-06: keep `small.en`.** Real benchmark against `medium.en` and `distil-large-v3`, using the app's actual production `transcribe_filtered()` call and settings: both alternatives cost a real ~3-4x latency penalty (a ~6-8s wait becomes ~15-20s on the 20-55s segments real interview answers routinely produce) and fixed zero of `small.en`'s errors — all three models made the same genuine homophone misses ("read"/"red", "write"/"right"), and both bigger models added new errors of their own. The production downsample step and `VoiceSegmenter` were both sanity-checked against real audio and ruled out as contributing causes. **Real caveat, not settled by this test:** all test audio was clean TTS, and the errors found are exactly the kind a bigger model doesn't fix — this doesn't rule out a bigger checkpoint helping on real noisy/accented meeting audio, only that there's no free win on clean speech. A real recorded (non-TTS) sample would be the actual next step if this needs revisiting; not pursued for now since Day 18.6/18.7 address related accuracy concerns from other angles. See DEV_PLAN Day 18.5.
- **Suggestion format — script + bullets:** suggestions become a short 1-2 sentence lead (the "script" framing), followed by 3-5 bullet points of supporting details/angles — closer to how the real ParakeetAI presents suggestions, letting the user pick and choose what to actually say rather than reading a fixed script verbatim. Applied to both modes Day 15. **Revised Day 18, Interview mode only:** a real example (a full behavioral-interview answer — narrative lead, bulleted specifics, a closing line, formal tone) showed the Day 15 format was too terse for this use case. Interview mode's suggestions now use a longer, balanced-length, formal narrative-plus-bullets structure; Work Meeting mode keeps Day 15's shorter, casual style unchanged.
- **Fast-speech live preview — bringing `base.en` streaming back, ephemeral display only, idea proposed 2026-09-05.** Jon's real-world finding: `small.en`'s whole-segment decode is accurate when speech has natural pauses, but on fast, continuous speech (now up to 60s with nothing shown until the segment closes), the eventual result "does not get anything close" to correct — whereas the reverted `base.en` streaming pipeline, imperfect as it was, "actually gets something" live during that same stretch. Two distinct claims here, treated separately rather than assumed together: showing *something* live during a long fast-speech stretch is a safe UX win; whether `base.en`'s settled output is actually *more accurate* than `small.en`'s on fast speech specifically is a real, testable claim, not a given. **Design constraint that keeps this safe to build, unlike the reverted "polish hybrid":** `base.en` streaming comes back strictly as an ephemeral, display-only preview shown while a segment is still open — the moment `small.en` finishes the authoritative whole-segment decode, that text replaces the preview outright, never merged or reconciled with it. The three Day 18 bugs all came from *merging* two models' output into one committed text; a preview that gets fully swapped, never blended, doesn't share that failure mode. See DEV_PLAN Day 18.6, sequenced right after the STT model-size spike (Day 18.5) and before the continuous context feed idea below (now Day 18.7).
  **Resolved 2026-09-06: hold off, not implemented.** Real testing found the accuracy claim false, and specifically worse on the exact scenario it targets: on a 39.5s continuous fast-speech clip (confirmed to stay one VAD segment, matching the real target scenario), roughly half of `base.en`'s settled transcript was simply missing — real content loss in the LocalAgreement-2 commit step, the same structural defect Day 16 first found and Day 18 already reverted away from, reproduced again here. A live viewer would see words appear and then get erased, which is worse than showing nothing. It also fell further behind realtime the longer the segment ran (1.41x realtime factor), and running it alongside `small.en` slowed `small.en`'s own segment decode by a real ~1.53x — a direct tax on the pipeline that matters more. One honest caveat: the same clean-TTS test of `small.en` on that clip stayed structurally complete, not reproducing the severe breakdown the original complaint described — either a real-audio-quality gap this synthetic test can't capture, or specific to audio this test didn't hit. **Untried, more promising angle, not committed to any day:** a fast-speech-aware segmentation change (shorter sub-segments triggered by detected fast cadence, still just `small.en`, still one decode per segment) rather than a second streaming model.
- **Continuous context feed for suggestions — idea proposed 2026-09-05, spike scheduled.** Today, `MeetingSession` rebuilds a rolling window of recent transcript text and pastes it fresh into the prompt at trigger time. Jon's proposal: instead, feed each finished transcript segment into the long-lived `claude` CLI session as it happens, so by the time a suggestion is triggered the model already has the whole meeting in its own turn history, and the trigger only needs a small fixed nudge ("answer the last question") rather than a reconstructed blob of text. Reasoning: even where the literal transcript has real errors, an LLM that's been following the whole conversation should generally be able to work out the intended question, similar to how a person listening the whole time can recover a mumbled word from context — this is a different, complementary mechanism from raw STT accuracy (see the model-size item above), not a substitute for it. One real limit worth being explicit about: this compensates for garbled-but-recognizably-off transcription, not for a confidently fluent but wrong one — the exact failure mode Day 18's hallucination filters were built to catch, where nothing about the text signals it might be wrong. Two genuinely open engineering questions before this gets built: whether the CLI's headless mode can ingest a turn as pure context without forcing a full response every single time, and how subprocess recycling (today sized around suggestion-call volume only, every 10 `ask()` calls) needs to change once transcript segments — arriving far more often — also become turns. See DEV_PLAN Day 18.7, sequenced after the fast-speech live preview work (Day 18.6) and before mic capture (Day 19), since dual-source ("You:"/"Them:") labeling will build on whatever context mechanism this settles on.
  **Resolved 2026-09-06: hold off, don't build.** Confirmed against the real `claude` CLI: every `ask()` call forces a full paid generation, there's no way to feed a turn as pure context, and the proposed fallback (batch several segments into one silent turn) turns out to just be today's rolling-window design again — this alone forecloses the idea as described. A real side-by-side test (genuine `small.en` transcription errors, real timing) found no accuracy benefit anyway, at a real ~6-8x compute cost that widens the longer a meeting runs, and confirmed the hallucination caveat stays unfixed either way — this spike's own test data (3 real transcription errors) went unnoticed by both approaches, underscoring that Day 18's `no_speech_prob`/`avg_logprob` filters remain the only real defense against a fluent-but-wrong transcription. **Cheap alternative lever flagged, bundled into Day 19 instead:** `SEGMENTS_TO_KEEP_FOR_CONTEXT` is a fixed count (10), not a size budget — raising it or making it size-based adds real history to the one call already happening for nearly free, worth revisiting once mic capture doubles the segment rate.
- **Live-agent-listening — idea proposed 2026-09-06, paused pending a direction decision.** A follow-up to the continuous context feed idea above: instead of the scripted headless `claude -p` subprocess, keep an actual interactive Claude Code agent session attached to a meeting, watching the transcript live and reasoning about it continuously. A real feasibility test (Phase 1, self-contained) confirmed the appeal — near-zero latency at trigger time, real transcription errors correctly recovered from context — but also confirmed a real structural problem: this only works while a full-tool-access, human-attended agent session is actively watching for the whole meeting, which is not something `windows_app`/`wsl_app` can invoke or run unattended. That's a different deployment model, not an increment on the current architecture, and it conflicts with why `ClaudeCli` was built headless in the first place (Day 6) and with the "chatty tool could hit subscription rate limits" risk already in §10. A cheaper, buildable alternative worth testing first: instruct the existing unattended `ClaudeCli` subprocess, via its system prompt, to reply trivially to ordinary segment turns and only elaborate on the trigger turn — this stays inside the current architecture, though it wouldn't reproduce the near-zero trigger latency, since that came specifically from context already being live in an already-running session, not from anything a discrete subprocess call can shortcut. See DEV_PLAN Day 18.8 — paused, not proceeding to a live test until Jon decides between the attended-session direction, the cheap-ack redesign, or leaving this aside.
  **Decided 2026-09-06: pursue the attended-session mode as a real second mode of the app.** Operational model confirmed: Jon starts a Claude Code session on his own machine before a real meeting and leaves it running fully hands-off for the whole thing, no typing into it during the meeting — Phase 1 already demonstrated this shape. Two real questions still open before this becomes a shipped feature, not yet independently confirmed: whether a bare terminal `claude` session (no IDE) exposes the same free/cheap background-notification tooling Phase 1's VSCode-hosted session used, and whether it holds up fully unattended over a real 30-60 minute meeting rather than the ~60-second narrated test Phase 1 ran. See DEV_PLAN Day 18.9 — a check only Jon can run (start a bare terminal session, walk away, come back), answering the first question; the second needs a separate longer test afterward.
  **Resolved 2026-09-06: question 1 confirmed, pass.** Jon ran the check himself in a plain terminal (no IDE), fully unattended (`claude --permission-mode bypassPermissions` so it wouldn't stall on an approval prompt with nobody there to answer). The mechanism held: a background task with output arriving as live notifications, no polling, no human relay — not a VSCode-extension artifact or anything cloud-specific. The harness ran to completion with zero intervention and a real, correctly-grounded suggestion came out the other end. **Question 2 (real-duration robustness) is still open** — this run was still the same ~46s clip, just unattended instead of narrated. See DEV_PLAN Day 18.10, the real-duration test that's the actual bar for calling this a viable second mode, given it also exercises context growth and subscription rate-limit risk (PRD §10) that a 46-second run can't.
  **Resolved 2026-09-07: pass, real ~50-minute unattended run.** Jon ran the full harness (built and smoke-tested 2026-09-06) for real — 49.9 min wall clock, all 9 cycles plus the 3 five-minute idle stretches, exited cleanly via its own schedule-done path, no crash. All four open questions answered directly from the log: no degradation (141/141 segments transcribed, 141/141 triggers fired, per-cycle transcribe latency flat at 2.42-2.51s from cycle 1 through cycle 9); a real turn-count estimate (141 notifications over ~50 minutes, the number to reason about PRD §10's subscription rate-limit risk against); zero errors or stalls; and the 3 silent stretches produced zero spurious segments, confirming Day 18's hallucination filters hold up under real extended silence. **This closes the live-agent-listening investigation (Day 18.8-18.10) with a real pass** — question 1 (Day 18.9, bare-terminal parity) and question 2 (Day 18.10, real-duration robustness) are both confirmed. Whether and when this becomes a shipped second mode of the app (versus the existing scripted headless `ClaudeCli` mode) is a separate product decision, not yet scheduled.

### Phase 3 — Summary
- **Mic capture pulled forward, 2026-09-05:** originally last in this phase, now moved up ahead of Phase 2's opt-in persistence (DEV_PLAN Day 19, before the old Day 19) — Jon wants his own voice captured sooner rather than waiting for the rest of Phase 3. Own mic input alongside the existing loopback capture, decoded and transcribed the same way, with the two sides shown as separate labeled transcript lines ("You:" / "Them:") rather than merged — keeps both sides distinguishable for suggestions now and for the summary feature below later. Bundled in from Day 18.7's flagged cheap lever: `SEGMENTS_TO_KEEP_FOR_CONTEXT` (a fixed count of 10 today) gets revisited here too, since a two-source session fills it roughly twice as fast as one-source did.
  **Implemented and real-tested, 2026-09-07 (DEV_PLAN Day 19).** Both sides reworked for two fully independent sources: `wsl_app` gives mic and loopback each their own `VoiceSegmenter`, tags every transcribed segment with its source and real start time, and orders the rolling context Claude sees (and the transcript pane, on the `windows_app` side) by actual spoken order rather than transcription-completion order. The fixed-count context window became a character budget (`CONTEXT_CHARACTER_BUDGET`) per the flagged lever. `windows_app` gained a second device picker/capture manager for the mic, with a graceful loopback-only fallback on a machine with no usable microphone. Jon's real live test confirmed dual-source labeling works, but found 3 real bugs: mic room-noise transcribed as Whisper-hallucinated gibberish (fixed: stricter VAD for mic only), transcription latency ballooning to 15-35s under a noise-triggered segment burst (fixed: a lock serializing actual `transcribe()` calls across sources, since concurrent decode was real CPU contention, not real parallelism — matches Day 18.6's finding), and — the most significant — suggestions reading like meta-instructions/outlines instead of real content, root-caused to two things: the `claude` CLI subprocess was auto-loading this project's own `CLAUDE.md` into every suggestion (a latent bug since Day 6, fixed by giving the subprocess a neutral `cwd`), and both mode instructions could still produce outline-style lines even once that leak was fixed (fixed with a new shared instruction using Jon's own real example as an in-prompt contrast). The suggestion-format fix is live-verified against the real `claude` CLI with varied inputs; the mic-noise and concurrency fixes still need a real mic session to confirm. See DEV_PLAN Day 19 for full detail.
- Post-meeting summary generation (key points, decisions, action items) via the Claude CLI.
- Export summary to markdown/txt.

### Phase 4 — Possible future work (not committed)
- Speaker diarization improvements, meeting-platform-specific hooks, a session history browser.
- **Cloud STT (e.g. OpenAI's Whisper API) as a nice-to-have, added 2026-09-05.** Only worth revisiting if the local accuracy work in Phase 2.5 (bigger local checkpoint, see above) doesn't get English accuracy where Jon wants it. This would mean accepting a per-minute cloud cost and breaking PRD §2's "no per-token API billing" goal for STT specifically — a conscious tradeoff to make later if local options prove insufficient, not something to reach for now. Explicitly deprioritized: local accuracy improvements come first.

## 9. Non-Functional Requirements

- **Latency:** suggestion visible within **~5–8 seconds** of trigger firing, after the first turn of a session (first turn may be slower due to CLI startup). **Resolved Day 10** — this target was ~2-4s originally, then flagged as a likely 8-12s+ problem from Day 6's isolated component measurements (STT 2.4–6.1s, Claude CLI 4.02–6.23s). Day 10's real 17-minute end-to-end session measured actual pause-to-suggestion at 3-5s in the typical case (STT dropped to 1.4-2.1s under real conditions; the pause timer + CLI call make up the rest), for a realistic total of ~5-8s. Decision: accept ~5-8s as the target rather than trading real cost (smaller Whisper model, a tighter prompt) for a smaller gap than assumed. The occasional 10-20s+ outlier tracked to `claude` CLI subprocess-recycling CPU contention, not model size or prompt length — a scheduling issue, not a latency-budget one; worth a future look but not a Day 10/11 blocker.
- **Resource usage:** must not noticeably degrade the actual meeting app's performance; STT model size is adjustable to trade accuracy for CPU/GPU load.
- **Reliability:** a crashed CLI subprocess should be detected and restarted without losing the live transcript already captured.
- **Privacy:** no data leaves the machine except the Claude CLI's own calls under your existing authenticated session; no telemetry, no analytics.
- **Packaging:** runs as a dev tool from source (Python venv) — no installer needed.

## 10. Risks & Mitigations

- **Subscription rate limits:** a chatty real-time tool could hit Pro/Max usage limits faster than normal chat use → mitigated by trigger-gating (not a suggestion every few seconds) and graceful in-app handling of auth/rate-limit errors.
- **STT accuracy** on accented speech, crosstalk, or poor audio → adjustable model size, manual re-trigger as fallback.
- **WASAPI loopback quirks** (device changes, default device switching mid-call) → explicit device selection + test-capture button rather than always trusting the OS default.
- **CLI streaming mode is being used outside its typical interactive use** → its exact behavior (rate limits, stream-json stability, session semantics) needs to be verified empirically early, before the rest of the build depends on it. This should be the very first spike in the dev plan, not assumed.
- **Legal exposure** from persisted recordings → default-off persistence in Phase 1, addressed deliberately in Phase 2.
- **Two-process architecture adds real failure surface**: socket disconnects, one side crashing while the other keeps running, WSL2 localhost-forwarding quirks, a possible first-run Windows Firewall prompt for the native process → explicit reconnect/restart handling is now its own dev-plan day (see Day 9) rather than an afterthought.
- **All testing to date (Days 4-11) used TTS/synthetic playback, never a real Zoom/Teams call.** Day 11 found that a real USB headset's WASAPI loopback stops delivering audio callbacks during *any* silence gap when the source is discrete TTS renders (each `Speak()` call opens/closes its own render session) — a real meeting app very likely holds one continuously-open stream instead, which should behave differently, but this hasn't been directly confirmed → do a first real-call test early, before relying on this for anything high-stakes like a real interview.

## 12. Day 0 Feasibility Log

- ✅ `claude` CLI headless stream-json mode confirmed viable (`--input-format stream-json --output-format stream-json` exist and work; `-p ... --output-format json` returns clean JSON). Architecture in §6 builds on this directly.
- ✅ Subscription auth confirmed (`oauthAccount` in `~/.claude.json`, no `ANTHROPIC_API_KEY` set). `total_cost_usd` in CLI JSON output is list-price telemetry only, not an actual charge.
- ❌ WSL-native audio loopback capture does not work and structurally can't via WSLg's PulseAudio bridge (`RDPSink.monitor` stayed `SUSPENDED`, recorded pure silence — confirmed via `sox stat`, not just assumed). This is the finding that forced the §6 architecture revision.
- Repo created: `github.com/jjaguilar08/adarna-ai` (private), local clone at `/home/jon/projects/adarna-ai`, git initialized, nothing committed yet.
- Python 3.10.12 confirmed sufficient — no upgrade needed (see §7, decision 9).

## 11. Success Criteria (MVP Definition of Done)

**✅ Met — Day 11 (2026-09-03).** All four criteria confirmed in one real, unbroken ~15-minute end-to-end session, hotkey included (4/4 real presses, 4/4 suggestions in 2-3s each).

- Can start the app during a real Windows work meeting (audio via speakers/headphones) and see a live rolling transcript of the other side of the conversation.
- A suggestion appears in-window within a few seconds of either a detected pause or a manual hotkey press.
- Runs through a 15–30 minute mock session end-to-end without crashing or requiring a restart.
- No audio or transcript is written to disk unless explicitly enabled.

---

**Next steps:** once you sign off on this (and the open decisions in §7), I'll write a day-by-day dev plan, starting with a Day 0/1 spike specifically to validate the `claude` CLI's streaming headless behavior under subscription auth — since the whole latency story in §6 depends on that working the way I've assumed.
